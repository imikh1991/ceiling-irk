export type Inputs = {
    name: string;
    address: string;
};

export type IAdvCard = {
    title: string;
    img: string;
    description: string;
    alt: string;
    href: string;
};

export type WorkItem = {
    id: string;
    title: string;
    description: string;
    img: string;
    alt: string;
};
